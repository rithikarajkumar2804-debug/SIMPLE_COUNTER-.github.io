# SIMPLE_COUNTER-.github.io
Counter App is a simple web application built using HTML, CSS, and JavaScript (DOM). It allows users to increase, decrease, and reset a counter value by clicking buttons. The app demonstrates how JavaScript interacts with HTML elements using the DOM and responds to user actions through event listeners.
